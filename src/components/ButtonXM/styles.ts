import { StyleSheet } from 'react-native';

export const buttonStyles = StyleSheet.create({
    container: {
        borderRadius: 30,
        overflow: 'hidden',
        marginBottom: 10,
        width: 300,
        height: 60,
        alignItems: 'center',
        justifyContent: 'center',
    },
    title: {
        fontSize: 20,
    },
});



