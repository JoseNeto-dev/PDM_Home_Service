import { StyleSheet } from 'react-native';
import { globalTheme } from '../../global/styles/themes';

export const styles = StyleSheet.create({
    buttonVoltar:{
        fontSize: globalTheme.SIZE.lg,
        color: globalTheme.COLORS.purple700
    }
})